/*SessionStorage exists only within the current browser, tab another tab with same page will have different storage. 
   When refresh the page data is there but when closed or reopen the tab data is not there.*/

// storage event: when the data gets updated in localstorage or sessionstorage, storage event trigger with these properties.

